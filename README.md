Get the logs on Cloudwatch
`/aws/lambda/collect-emails-dev-confirm`
Change ENV Variables under Lambda console

DynamoDB table for `mtml-emails`, should probably update email table name to `#{project}-email`
